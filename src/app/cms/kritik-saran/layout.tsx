import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Inbox Kritik & Saran",
}

export default function KritikSaranLayout({ children }: { children: React.ReactNode }) {
  return children
}
