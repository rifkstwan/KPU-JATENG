import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Pengaturan Profil KPU",
}

export default function TentangLayout({ children }: { children: React.ReactNode }) {
  return children
}
